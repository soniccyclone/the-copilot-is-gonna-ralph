# Implementation task — issue #${PARENT_ISSUE}

You are implementing the feature described in the merged design doc at:
`${DESIGN_PATH}`

Read both:
- `docs/planning/${PARENT_ISSUE}/prd.md` for the goals and constraints
- `docs/planning/${PARENT_ISSUE}/design.md` for the chosen approach and step-by-step plan

## What "done" means

Every item in the design doc's "Step-by-step implementation plan" is implemented and tested. Tests pass. Code follows the conventions already in this repo (read existing files before adding new ones — match the local style).

## Operating rules

- You are running in an autonomous loop. Each iteration sees the history of previous iterations. If something failed last time, try a different approach.
- Implement against the design. Don't redesign mid-implementation. If the design is wrong, document the gap in your iteration log and propose a fix — but stay aligned with the PRD's goals.
- Run the project's tests after each meaningful change. Don't claim done until tests pass.
- Don't touch `docs/planning/${PARENT_ISSUE}/` — the PRD and design doc are immutable inputs.
- Don't run `git commit` or `git push` — the workflow handles git after the loop completes.
- When the entire implementation plan is complete and tested, create the file `.ralph/DONE` to signal completion.
